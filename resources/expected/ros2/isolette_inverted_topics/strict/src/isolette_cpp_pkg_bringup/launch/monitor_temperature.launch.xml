<!-- Content between markers will be preserved if codegen is rerun -->

<launch>
    <!-- LAUNCH ARGUMENTS - additions within these tags will be preserved when re-running Codegen -->
    <!-- Declare launch arguments here, then reference them from a node's parameters, e.g.
             <arg name="log_file" default="uros-demo.txt"/>
         with, inside that node's config block:
             <param name="log_file_name" value="$(var log_file)"/> -->
    <!-- LAUNCH ARGUMENTS - additions within these tags will be preserved when re-running Codegen -->


    <node pkg="isolette_cpp_pkg" exec="thermostat_monitor_temperature_manage_alarm_mat_exe">
        <!-- LAUNCH CONFIG thermostat_monitor_temperature_manage_alarm_mat - additions within these tags will be preserved when re-running Codegen -->
        <!-- LAUNCH CONFIG thermostat_monitor_temperature_manage_alarm_mat - additions within these tags will be preserved when re-running Codegen -->
    </node>


    <node pkg="isolette_cpp_pkg" exec="thermostat_monitor_temperature_manage_monitor_interface_mmit_exe">
        <!-- LAUNCH CONFIG thermostat_monitor_temperature_manage_monitor_interface_mmit - additions within these tags will be preserved when re-running Codegen -->
        <!-- LAUNCH CONFIG thermostat_monitor_temperature_manage_monitor_interface_mmit - additions within these tags will be preserved when re-running Codegen -->
    </node>


    <node pkg="isolette_cpp_pkg" exec="thermostat_monitor_temperature_manage_monitor_mode_mmmt_exe">
        <!-- LAUNCH CONFIG thermostat_monitor_temperature_manage_monitor_mode_mmmt - additions within these tags will be preserved when re-running Codegen -->
        <!-- LAUNCH CONFIG thermostat_monitor_temperature_manage_monitor_mode_mmmt - additions within these tags will be preserved when re-running Codegen -->
    </node>

</launch>
