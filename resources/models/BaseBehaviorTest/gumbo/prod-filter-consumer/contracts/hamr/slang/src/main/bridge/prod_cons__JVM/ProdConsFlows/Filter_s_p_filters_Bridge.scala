// #Sireum

package prod_cons__JVM.ProdConsFlows

import org.sireum._
import art._
import prod_cons__JVM._
import prod_cons__JVM.ProdConsFlows.{Filter_s_p_filters => component}

// Do not edit this file as it will be overwritten if HAMR codegen is rerun

@datatype class Filter_s_p_filters_Bridge(
  val id: Art.BridgeId,
  val name: String,
  val dispatchProtocol: DispatchPropertyProtocol,
  val dispatchTriggers: Option[ISZ[Art.PortId]],

  a_data_in: Port[ProdConsFlows.Container_i],
  e_data_out: Port[ProdConsFlows.Container_i],
  b_event_data_in: Port[ProdConsFlows.Container_i],
  c_event_data_in: Port[ProdConsFlows.Container_i],
  f_event_data_out: Port[ProdConsFlows.Container_i],
  g_event_data_out: Port[ProdConsFlows.Container_i],
  d_event_in: Port[art.Empty],
  h_event_out: Port[art.Empty]
  ) extends Bridge {

  val ports : Bridge.Ports = Bridge.Ports(
    dataIns = ISZ[art.UPort](a_data_in),

    dataOuts = ISZ[art.UPort](e_data_out),

    eventIns = ISZ[art.UPort](b_event_data_in,
                              c_event_data_in,
                              d_event_in),

    eventOuts = ISZ[art.UPort](f_event_data_out,
                               g_event_data_out,
                               h_event_out)
  )

  val initialization_api : Filter_s_Initialization_Api = {
    val api = Filter_s_Initialization_Api(
      id,
      a_data_in.id,
      e_data_out.id,
      b_event_data_in.id,
      c_event_data_in.id,
      f_event_data_out.id,
      g_event_data_out.id,
      d_event_in.id,
      h_event_out.id
    )
    Filter_s_p_filters_Bridge.c_initialization_api = Some(api)
    api
  }

  val operational_api : Filter_s_Operational_Api = {
    val api = Filter_s_Operational_Api(
      id,
      a_data_in.id,
      e_data_out.id,
      b_event_data_in.id,
      c_event_data_in.id,
      f_event_data_out.id,
      g_event_data_out.id,
      d_event_in.id,
      h_event_out.id
    )
    Filter_s_p_filters_Bridge.c_operational_api = Some(api)
    api
  }

  val entryPoints : Bridge.EntryPoints =
    Filter_s_p_filters_Bridge.EntryPoints(
      id,

      a_data_in.id,
      e_data_out.id,
      b_event_data_in.id,
      c_event_data_in.id,
      f_event_data_out.id,
      g_event_data_out.id,
      d_event_in.id,
      h_event_out.id,

      dispatchTriggers,

      initialization_api,
      operational_api)
}

object Filter_s_p_filters_Bridge {

  var c_initialization_api: Option[Filter_s_Initialization_Api] = None()
  var c_operational_api: Option[Filter_s_Operational_Api] = None()

  @datatype class EntryPoints(
    Filter_s_p_filters_BridgeId : Art.BridgeId,
    a_data_in_Id : Art.PortId,
    e_data_out_Id : Art.PortId,
    b_event_data_in_Id : Art.PortId,
    c_event_data_in_Id : Art.PortId,
    f_event_data_out_Id : Art.PortId,
    g_event_data_out_Id : Art.PortId,
    d_event_in_Id : Art.PortId,
    h_event_out_Id : Art.PortId,
    dispatchTriggers : Option[ISZ[Art.PortId]],
    initialization_api: Filter_s_Initialization_Api,
    operational_api: Filter_s_Operational_Api) extends Bridge.EntryPoints {

    val dataInPortIds: ISZ[Art.PortId] = IS(a_data_in_Id)

    val eventInPortIds: ISZ[Art.PortId] = IS(b_event_data_in_Id,
                                             c_event_data_in_Id,
                                             d_event_in_Id)

    val dataOutPortIds: ISZ[Art.PortId] = IS(e_data_out_Id)

    val eventOutPortIds: ISZ[Art.PortId] = IS(f_event_data_out_Id,
                                              g_event_data_out_Id,
                                              h_event_out_Id)

    def initialise(): Unit = {
      // implement the following method in 'component':  def initialise(api: Filter_s_Initialization_Api): Unit = {}
      component.initialise(initialization_api)
      Art.sendOutput(eventOutPortIds, dataOutPortIds)
    }

    def compute(): Unit = {
      // transpiler friendly filter
      def filter(receivedEvents: ISZ[Art.PortId], triggers: ISZ[Art.PortId]): ISZ[Art.PortId] = {
        var r = ISZ[Art.PortId]()
        val opsTriggers = ops.ISZOps(triggers)
        for(e <- receivedEvents) {
          if(opsTriggers.contains(e)) {
            r = r :+ e
          }
        }
        return r
      }

      // fetch received events ordered by highest urgency then earliest arrival-time
      val EventTriggered(receivedEvents) = Art.dispatchStatus(Filter_s_p_filters_BridgeId)

      // remove non-dispatching event ports
      val dispatchableEventPorts: ISZ[Art.PortId] =
        if(dispatchTriggers.isEmpty) receivedEvents
        else filter(receivedEvents, dispatchTriggers.get)

      Art.receiveInput(eventInPortIds, dataInPortIds)

      for(portId <- dispatchableEventPorts) {
        if(portId == b_event_data_in_Id){
          val Some(ProdConsFlows.Container_i_Payload(value)) = Art.getValue(b_event_data_in_Id)

          // implement the following in 'component':  def handle_b_event_data_in(api: Filter_s_Operational_Api, value: ProdConsFlows.Container_i): Unit = {}
          component.handle_b_event_data_in(operational_api, value)
        }
        else if(portId == c_event_data_in_Id){
          val Some(ProdConsFlows.Container_i_Payload(value)) = Art.getValue(c_event_data_in_Id)

          // implement the following in 'component':  def handle_c_event_data_in(api: Filter_s_Operational_Api, value: ProdConsFlows.Container_i): Unit = {}
          component.handle_c_event_data_in(operational_api, value)
        }
        else if(portId == d_event_in_Id) {
          // implement the following in 'component':  def handle_d_event_in(api: Filter_s_Operational_Api): Unit = {}
          component.handle_d_event_in(operational_api)
        }
      }

      Art.sendOutput(eventOutPortIds, dataOutPortIds)
    }

    def activate(): Unit = {
      // implement the following method in 'component':  def activate(api: Filter_s_Operational_Api): Unit = {}
      component.activate(operational_api)
    }

    def deactivate(): Unit = {
      // implement the following method in 'component':  def deactivate(api: Filter_s_Operational_Api): Unit = {}
      component.deactivate(operational_api)
    }

    def finalise(): Unit = {
      // implement the following method in 'component':  def finalise(api: Filter_s_Operational_Api): Unit = {}
      component.finalise(operational_api)
    }

    def recover(): Unit = {
      // implement the following method in 'component':  def recover(api: Filter_s_Operational_Api): Unit = {}
      component.recover(operational_api)
    }

    override
    def testInitialise(): Unit = {
      // implement the following method in 'component':  def initialise(api: Filter_s_Initialization_Api): Unit = {}
      component.initialise(initialization_api)
      Art.releaseOutput(eventOutPortIds, dataOutPortIds)
    }

    override
    def testCompute(): Unit = {
      // transpiler friendly filter
      def filter(receivedEvents: ISZ[Art.PortId], triggers: ISZ[Art.PortId]): ISZ[Art.PortId] = {
        var r = ISZ[Art.PortId]()
        val opsTriggers = ops.ISZOps(triggers)
        for(e <- receivedEvents) {
          if(opsTriggers.contains(e)) {
            r = r :+ e
          }
        }
        return r
      }

      // fetch received events ordered by highest urgency then earliest arrival-time
      val EventTriggered(receivedEvents) = Art.dispatchStatus(Filter_s_p_filters_BridgeId)

      // remove non-dispatching event ports
      val dispatchableEventPorts: ISZ[Art.PortId] =
        if(dispatchTriggers.isEmpty) receivedEvents
        else filter(receivedEvents, dispatchTriggers.get)

      Art.receiveInput(eventInPortIds, dataInPortIds)

      for(portId <- dispatchableEventPorts) {
        if(portId == b_event_data_in_Id){
          val Some(ProdConsFlows.Container_i_Payload(value)) = Art.getValue(b_event_data_in_Id)

          // implement the following in 'component':  def handle_b_event_data_in(api: Filter_s_Operational_Api, value: ProdConsFlows.Container_i): Unit = {}
          component.handle_b_event_data_in(operational_api, value)
        }
        else if(portId == c_event_data_in_Id){
          val Some(ProdConsFlows.Container_i_Payload(value)) = Art.getValue(c_event_data_in_Id)

          // implement the following in 'component':  def handle_c_event_data_in(api: Filter_s_Operational_Api, value: ProdConsFlows.Container_i): Unit = {}
          component.handle_c_event_data_in(operational_api, value)
        }
        else if(portId == d_event_in_Id) {
          // implement the following in 'component':  def handle_d_event_in(api: Filter_s_Operational_Api): Unit = {}
          component.handle_d_event_in(operational_api)
        }
      }

      Art.releaseOutput(eventOutPortIds, dataOutPortIds)
    }
  }
}